# ── Secrets & runtime config (contain credentials — never commit) ──
.env
devices.json
node-overrides.json
checks.json

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ── Claude Code local settings ──
.claude/settings.local.json

# ── OS / editor junk ──
.DS_Store
Thumbs.db
*.swp
*.log

# ── Scratch (backup migrasi Python, screenshot bug) ──
monitor-api-python-backup.tar.gz
ss.png
